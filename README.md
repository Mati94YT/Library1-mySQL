It's a simple library service written in ASP.NET, designed for MySQL. To use it you only need created "library" database in MySQL server (I recommend phpMyAdmin)
All tables will be created automatically. For now, it only allows users to add books (they can't see them), but more features will be added in future
